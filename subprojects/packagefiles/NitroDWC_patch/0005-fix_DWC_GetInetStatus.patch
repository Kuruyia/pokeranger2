diff --git a/libraries/base/src/dwc_connectinet.c b/libraries/base/src/dwc_connectinet.c
index 6c91cdc..4caf906 100644
--- a/libraries/base/src/dwc_connectinet.c
+++ b/libraries/base/src/dwc_connectinet.c
@@ -169,7 +169,6 @@ void DWC_ProcessInet (void)
 DWCInetResult DWC_GetInetStatus (void)
 {
     int state;
-    static int stateOld;
     DWCInetResult result = DWC_CONNECTINET_STATE_NOT_INITIALIZED;
 
     if (stpInetCntl == NULL) {
@@ -190,14 +189,10 @@ DWCInetResult DWC_GetInetStatus (void)
         stpInetCntl->online = TRUE;
     } else if (state < 0) {
         if (state >= -10) {
-            DWCi_SetError(DWC_ERROR_FATAL, -50199);
+            DWCi_SetError(DWC_ERROR_FATAL, state - 0x2BC);
             result = DWC_CONNECTINET_STATE_FATAL_ERROR;
             stpInetCntl->state = result;
         } else {
-            if (stateOld < state) {
-                state = stateOld;
-            }
-
             DWCi_SetError(DWC_ERROR_AC_ANY, state);
 
             result = DWC_CONNECTINET_STATE_ERROR;
